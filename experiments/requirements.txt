# AAPA-MQTT testbed dependency lock for the registered `firstpaper` environment.
# Install with conda:
#   conda activate firstpaper
#   pip install -r requirements.txt
#   pip install /path/to/liboqs-python/  # local binding, version recorded by preflight
#
# Native prerequisites (recorded, but not installed by pip):
#   - liboqs C library (compiled to /usr/local/lib/)
#   - mosquitto MQTT broker

paho-mqtt==2.1.0
cryptography==48.0.0
pandas==3.0.3
numpy==2.4.6
matplotlib==3.10.9
seaborn==0.13.2
